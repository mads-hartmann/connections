{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "connections",
  "title": "Connections",
  "description": "Manage your connections and their RSS feeds",
  "icon": "extension-icon.png",
  "author": "mads",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Other"
  ],
  "license": "MIT",
  "preferences": [
    {
      "name": "serverUrl",
      "title": "Server URL",
      "description": "URL of the Connections server",
      "type": "textfield",
      "required": true,
      "default": "http://localhost:8080"
    },
    {
      "name": "defaultView",
      "title": "Default View",
      "description": "Which view to show by default when opening Connections",
      "type": "dropdown",
      "required": false,
      "default": "connections",
      "data": [
        { "title": "Connections", "value": "connections" },
        { "title": "Articles", "value": "articles" },
        { "title": "Tags", "value": "tags" }
      ]
    }
  ],
  "commands": [
    {
      "name": "connections",
      "title": "Connections",
      "description": "Browse and manage your connections",
      "mode": "view"
    },
    {
      "name": "unread-count",
      "title": "Unread Articles",
      "description": "Shows unread article count in menu bar",
      "mode": "menu-bar",
      "interval": "5m"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.103.10",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build -o dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish",
    "publish-local": "npm run build && node -e \"const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('dist/package.json')); pkg.name = 'connections-live'; pkg.title = 'Connections - Live'; fs.writeFileSync('dist/package.json', JSON.stringify(pkg, null, 2));\""
  }
}
